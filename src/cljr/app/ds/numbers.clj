;; Clojure uses and supports Java boxed number types derived from
;; java.lang.Number and its own Ration
;; a) Integers
;; b) Ratio (22/7)
;; c) Float (operation involve double).

(ns cljr.app.ds.numbers)

;; computation related functions
;; + - * / inc dec quot rem min max

;; comparison related functions
;; == < <= > >= zero? pos? neg?

